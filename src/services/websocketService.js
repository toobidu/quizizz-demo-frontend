/**
 * WebSocket Service - Legacy Wrapper
 * 
 * File này giữ lại để tương thích ngược với code cũ.
 * Các component hiện tại vẫn import từ file này.
 * 
 * Trong tương lai, nên cập nhật các import để sử dụng:
 * import websocketService from './websocket';
 */

import websocketService from './websocket';
export default websocketService;